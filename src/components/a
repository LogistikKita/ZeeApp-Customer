
import React from 'react';

const Hero = () => {
  return (
    <section 
      id="hero" 
      className="relative h-screen bg-cover bg-center flex items-center"
      style={{ 
        // Ganti dengan URL gambar kamu
        backgroundImage: "url('/assets/images/background-lk.jpg')"
      }}
    >
      {/* Overlay Gelap/Kuning */}
      <div className="absolute inset-0 bg-black/50 backdrop-brightness-50"></div>
      
      {/* Konten Hero */}
      <div className="relative max-w-7xl mx-auto px-6 text-center text-white z-10">
        <h1 className="text-4xl md:text-6xl font-extrabold tracking-tight mb-4">
          SOLUSI LOGISTIK TERPADU DAN TERPERCAYA
        </h1>
        <p className="text-lg md:text-xl mb-8">
          Kami menyediakan layanan pengiriman barang yang cepat, aman, dan efisien ke seluruh Indonesia.
        </p>
        <a 
          href="#pricing" 
          className="px-8 py-3 bg-primary text-lg font-semibold rounded-full shadow-lg hover:bg-blue-600 transition duration-300"
        >
          SIMULASI HARGA & JADWAL
        </a>
      </div>
    </section>
  );
};

export default Hero;
